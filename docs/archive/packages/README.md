# Archived Packages

This directory contains historical ZIP snapshots of planning packages.

The extracted Markdown under [`../../plans/active/`](../../plans/active/README.md) is the maintained source. ZIP snapshots are retained only for provenance and must not be used as the current plan.
